# OpenVCS-SDK

Tooling for building and packaging OpenVCS plugins.

## `openvcs-plugin`
- Bundle a plugin into a single `.ovcsp` zip: `cargo run -p openvcs-sdk -- --plugin-dir /path/to/plugin`

## Development
- Before committing code: `cargo fmt`
